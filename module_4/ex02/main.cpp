#include "Cat.hpp"
#include "Dog.hpp"
#include "WrongCat.hpp"

int main()
{
	//Animal	animal;
	/*Animal*	PetShop[6];

	for (int i = 0; i < 3; i++)
		PetShop[i] = new Cat;
	for (int i = 3; i < 6; i++)
		PetShop[i] = new Dog;

	std::cout << std::endl;

	PetShop[0]->addIdea("I am hungry\n", 0);
	PetShop[0]->addIdea("Pet me human !\n", 1);
	std::cout << PetShop[0]->mindReading(1);
	std::cout << PetShop[0]->mindReading(0);
	std::cout << PetShop[4]->mindReading(0);

	std::cout << std::endl;

	for (int i = 0; i < 6; i++)
		delete PetShop[i];*/
	
	Dog basic;
	{
		Dog tmp = basic;
		std::cout << std::endl;
	}
    return 0;
}